Minor Fixes:
    - Make the "Switch Project" and "Back to Projects" buttons consistent (both "Back to Main Menu"), and a little more noticable (an actual button, rather than underlined text) (Addressed: both renamed to "Back to Main Menu" and restyled as real buttons.)
    - Hide API Configuration if Vandalizer mode is detected (Addressed.)
    - Hide "Total Budget — automatic extraction couldn't confirm a figure, please enter it from your spreadsheet" by default. The scan can happen right as the user uploads the budget document. Then, if we get past the try cap, then that field can appear. Or, if the scan validates, we can just print, "Total Budget: ${value}" in its place. (Addressed: the check-figure scan now starts the moment the budget file is selected; a resolved figure prints as "Total Budget: $value" in place, and the manual field only appears if the 5-round cap is exceeded.)
    - When viewing a calculated value and it's components, the actual calculated value should not be faded, and it should look different than its components. (Addressed: the calculated value now gets a solid, bold highlight distinct from the outlined highlight on its components, in both the normal view and while editing the formula.)
    - Local default institutional profiles aren't available in the generator dropdown. (Addressed: found the root cause — a broken `window.Generator` existence check in settings.js meant the dropdown was never re-synced after local profiles loaded; fixed. Follow-up: the dropdown was still defaulting to the user's own saved default instead of the local one, because Generator's first render ran before the local-profiles fetch resolved and then never re-checked; Settings.init() now awaits local profiles being loaded before the Generator initializes, so the local default wins from the first render.)
    - Lets go back to having two separate tabs for the Generator and the Verifier. But the system should port over the budget justification and spreadsheet from the generator to the verifier - if they uploaded one. (Addressed: split back into two tabs. Follow-up per later feedback: this is now fully automatic, no exceptions — the Verifier's spreadsheet and justification fields are filled in the moment the Generator's spreadsheet is uploaded or a justification is exported, with no button to click; uploading a different file directly in the Verifier still works as an explicit override.)
