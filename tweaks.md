Minor Fixes:
    - If the user highlights a value with no attributes in the budget justification, they should be able to set it as linked or calculated. (Addressed, per your choice of a toolbar button over a floating bubble: select any plain text that reads as a dollar amount, then click "Set Selection as Value" in the editor's header — it becomes a value chip and the inspector opens immediately so you can link or calculate it.) (User Follow Up: I don't see any toolbar button when I highlight a value.) (Re-addressed: the button was always there but easy to miss — it only changed opacity slightly when enabled, and the editor's modal header had no wrapping, so it could get visually cramped depending on window size. Now the button relabels itself to show the exact amount it detected (e.g. `Set "$5,000" as Value`) and switches to a solid, colored style the moment a valid selection is made, and the header row wraps instead of overflowing on narrow windows.)
    - After the user has generated a budget justification in the Generator, the main generator interface (Institutional Profile / Template header, file upload section) should be hidden. The user should have a big button to "View Generated Budget Justification" (this is the "Resume Editing Draft" button, just restyled and renamed). The "Show details" should be preserved. Then, there should be another button "Regenerate Budget Justification From Scratch" that allows them to reset the process. (Addressed: once a draft exists, the input form collapses in favor of "View Generated Budget Justification" and "Regenerate Budget Justification From Scratch"; "Show details"/the step log stays visible either way. Per your follow-up, Regenerate does a full blank-slate reset — draft, profile/template selection, and every uploaded file, Generator and Verifier alike — and asks for confirmation first since it's destructive.) (User Follow Up: Remove "Draft ready - continue editing below". Center the "View Generated Budget Justification" and "Regenerate Budget Justification from Scratch Buttons". Insert a background icon, large underneath the last element "Show details" that takes up space and makes things look prettier.) (Re-addressed: removed the "Draft ready" status message entirely, centered both buttons in the collapsed view, and added a large, faint 🧾 decoration underneath "Show details"/the log when a draft exists.)
