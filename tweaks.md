Minor Fixes:
    - Make it so that nothing is selected by default in the Generator - Grant Template Type dropdown. (Addressed: added a blank placeholder option; selecting one is now required before generating.)
    - If the user highlights a value with no attributes in the budget justification, they should be able to set it as linked or calculated. (Addressed, per your choice of a toolbar button over a floating bubble: select any plain text that reads as a dollar amount, then click "Set Selection as Value" in the editor's header — it becomes a value chip and the inspector opens immediately so you can link or calculate it.)
    - After the user has generated a budget justification in the Generator, the main generator interface (Institutional Profile / Template header, file upload section) should be hidden. The user should have a big button to "View Generated Budget Justification" (this is the "Resume Editing Draft" button, just restyled and renamed). The "Show details" should be preserved. Then, there should be another button "Regenerate Budget Justification From Scratch" that allows them to reset the process. (Addressed: once a draft exists, the input form collapses in favor of "View Generated Budget Justification" and "Regenerate Budget Justification From Scratch"; "Show details"/the step log stays visible either way. Per your follow-up, Regenerate does a full blank-slate reset — draft, profile/template selection, and every uploaded file, Generator and Verifier alike — and asks for confirmation first since it's destructive.)
    - I want the old Verifier interface and workflow back (the chat and markdown document). (Addressed per our follow-up: removed the "Open in Editor" hand-off button and the now-unused js/doc-ingest.js module it depended on. The Verifier again ends at the guided chat plus the downloadable marked-up document, nothing more. The automatic spreadsheet/justification carry-over from the previous round stays, since that wasn't the part you objected to.)
    - Uploaded documents in the drag and drop zones should have better styling in the Generator and Verifier. (Addressed: a selected file now renders as a file chip with an icon and a "Click or drop to change" hint, and the zone's border switches from dashed to solid.)
    - If a document is ported over to the Verifier by our system, the "Drag & drop" and extention text shouldn't show. (Addressed — turned out to be a real bug affecting every Verifier upload, not just ported-over ones: its drop-zone handler never hid that prompt at all, unlike the Generator's. Fixed to match.)

Raised mid-conversation (not in the list above, addressed alongside it):
    - Ask for confirmation before "Regenerate Budget Justification From Scratch" runs, since it's destructive. (Addressed — see above.)
    - Keep a history of Verifier runs the user can return to, unobtrusive but accessible, so re-checking is easy. (Addressed: a small "Verification History" link appears in the Verifier tab once at least one run has completed. It lists past runs (files, timestamp, clean/issue count) and lets you view any of them read-only — the live "Download Marked Up Document" action only applies to the current run, since older entries don't retain the original file. Kept per-project, capped at the last 20 runs.)